> **模板：L0 - 战略与愿景**
>
> **使用说明**
>
> 1. 替换 `{编号}`、`{文档名称}` 等占位符
> 2. 状态值从 `初稿/正式/草案/废弃` 中选择；**新建默认 `初稿`**（缺字段时按 `变更记录` 判定：有定稿条目 → `草案`，从未定稿 → `初稿`，无法判定 → `草案`）
> 3. **编码形式与项目前缀**：见文首说明
> 4. **本文档细项编码清单**：仅本文档范围；分配前检索当前作用域 `README.md` 全局索引，分配后同步更新
> 5. **细项定义块形态（MANDATORY）**：每个编码的定义位 MUST 写成四部分——`<a id="{编码全小写}"></a>` 锚点行 + `### {编码}：{标题}` 标题行（全角冒号）+ 属性行组 + 正文段；前三部分连续，正文段紧随属性行组并与它空一行；属性名 MUST 取自《属性行定义集（封闭）》，需成句表达的内容一律归正文段；MUST NOT 写成 `- **{编码}**：{描述}`；同类多项逐项重复完整块（房规见 [coding-system.md · 细项定义块形态](../../references/coding-system.md#细项定义块形态mandatory)、[属性行定义集（封闭）](../../references/coding-system.md#属性行定义集封闭)）
> 6. 维护细项编码清单和变更记录
>
> **正文边界**：`TEMPLATE:BEGIN` / `TEMPLATE:END` 之间为待复制正文；实例化时删除两行哨兵、只保留其间内容（房规见 [index.md · 模板边界（哨兵）](index.md#模板边界哨兵)）。

---

<!-- TEMPLATE:BEGIN -->
# L0-{编号}-{文档名称}

| 属性       | 值                         |
| ---------- | -------------------------- |
| 文档编码   | L0-{编号}                  |
| 层级       | L0 - 战略与愿景            |
| 版本       | v1.0                       |
| 状态       | 初稿    |
| 作者       | {当前用户.作者}                     |
| 创建日期   | YYYY-MM-DD                 |
| 最后更新   | YYYY-MM-DD                 |

> **编码形式**：默认使用简洁编码（`{类型码}-{三位序号}`，与正文占位符一致）。
> **项目前缀**：仅在当前作用域 `README.md` 已声明 `project_code` 且请求发起方明确确认启用时，在各类编码前加 `{项目编码}-` 前缀（细则见 `references/coding-system.md`）。

## 1. 愿景声明

用一到两段话描述产品/项目的终极目标和核心价值主张。

### 1.1 术语与概念（可选）

若战略/市场用语在本文有特定含义（如赛道名、产品代号），或需与全项目词汇表对齐，在此列出；无则写「无」或删本小节。

| 术语 | 定义 | 备注 |
|------|------|------|
| {术语} | {含义} | 可选：见项目词汇表路径 |

## 2. 商业背景

### 2.1 市场分析

描述目标市场、市场规模、竞争格局和市场机遇。

### 2.2 商业目标

<a id="gol-{编号}"></a>
#### GOL-{编号}：{目标名称}

- **细项状态**：初稿
- **修订版本号**：1
- **最后修订日期**：YYYY-MM-DD

{本目标要达成什么：一句话写明可量化的目标。}

- 衡量方式：{指标与判定口径}
- 适用范围：{本目标覆盖的业务边界}

> **逐项重复本块**：每个 `GOL` 独立成一个完整定义块，MUST NOT 用连续列表项罗列多个编码。

### 2.3 战略对齐

描述本项目与组织整体战略的关系和对齐方式。

## 3. 成功标准

| 指标     | 关联目标 | 目标值 | 衡量方式 |
| -------- | -------- | ------ | -------- |
| {指标}   | GOL-{编号} | {目标} | {方式} |

## 4. 关键假定与风险

<a id="asm-{编号}"></a>
### ASM-{编号}：{假定名称}

- **细项状态**：初稿
- **修订版本号**：1
- **最后修订日期**：YYYY-MM-DD

{设计所依赖的前提假设：一句话写明假定什么成立。}

- 适用范围：{依赖本假定的设计对象}
- 失效影响：{假定不成立时的影响}

<a id="rsk-{编号}"></a>
### RSK-{编号}：{风险名称}

- **细项状态**：初稿
- **修订版本号**：1
- **最后修订日期**：YYYY-MM-DD
- **概率**：高/中/低

{本风险是什么：一句话写明可能影响项目成功的风险因素。}

- 影响：{影响描述}
- 应对策略：{缓解或规避措施}

> **逐项重复本块**：每个 `ASM` / `RSK` 独立成一个完整定义块，MUST NOT 用连续列表项罗列多个编码。

## 5. 范围边界

<a id="con-{编号}"></a>
### CON-{编号}：{约束名称}

- **细项状态**：初稿
- **修订版本号**：1
- **最后修订日期**：YYYY-MM-DD
- **出处**：{外部给定方，如合同 / 法规 / 平台}

{本约束限定什么：一句话写明范围约束（包含 / 排除）。}

- 影响范围：{受本约束限制的设计对象}
- 不适用范围：{至少写明一条非目标}

> **逐项重复本块**：每个 `CON` 独立成一个完整定义块，MUST NOT 用连续列表项罗列多个编码。

---

## 本文档细项编码清单（仅列出本文档定义/包含）

> **清单范围**：仅收录**本文档**定义或包含的细项，**不是** `ued/` 下的全局完整清单。
> **分配流程**：分配编码前必须先检索当前作用域 `README.md` 的全局编码索引，确认无冲突后再分配；分配后同步更新该索引。
> **状态列**：`细项状态` 为必备列，取值 `初稿/正式/草案/废弃`，新建默认 `初稿`（缺字段判定同上）；锁定与解冻规则见 `references/status-definitions.md`。

| 编码       | 类型 | 标题     | 细项状态 |
| ---------- | ---- | -------- | -------- |
| GOL-{编号} | 目标 | {标题}   | 初稿    |
| ASM-{编号} | 假定 | {标题}   | 初稿    |
| RSK-{编号} | 风险 | {标题}   | 初稿    |
| CON-{编号} | 约束 | {标题}   | 初稿    |

## 变更记录

| 版本 | 日期       | 作者   | 变更说明 |
| ---- | ---------- | ------ | -------- |
| v1.0 | YYYY-MM-DD | {当前用户.作者} | 初始版本 |
<!-- TEMPLATE:END -->
