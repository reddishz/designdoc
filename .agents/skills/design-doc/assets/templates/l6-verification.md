> **模板：L6 - 验证与确认**
>
> **使用说明**
>
> 1. 替换 `{编号}`、`{文档名称}` 等占位符
> 2. 状态值从 `初稿/正式/草案/废弃` 中选择；**新建默认 `初稿`**（缺字段时按 `变更记录` 判定：有定稿条目 → `草案`，从未定稿 → `初稿`，无法判定 → `草案`）
> 3. **编码形式与项目前缀**：见文首说明
> 4. **本文档细项编码清单**：仅本文档范围；分配前检索当前作用域 `README.md` 全局索引，分配后同步更新
> 5. **细项定义块形态（MANDATORY）**：每个编码的定义位 MUST 写成「`<a id="{编码全小写}"></a>` 锚点行 + `### {编码}：{标题}` 标题行（全角冒号）+ `- **{要素名}**：{值}` 属性行」，MUST NOT 写成 `- **{编码}**：{描述}`；同类多项逐项重复完整块（房规见 [coding-system.md · 细项定义块形态](../../references/coding-system.md#细项定义块形态mandatory)）
> 6. 关联文档引用对应的 L2 和 L5 文档编码
> 7. 需求追溯矩阵的字段名为 `追溯状态`，从 `未追溯/已追溯/不适用` 中选择（记录型，与文档 `状态`、细项 `细项状态` 各自独立）；其中「验收标准 / 测试场景」列是**定义**，**不是执行记录**
>
> **正文边界**：`TEMPLATE:BEGIN` / `TEMPLATE:END` 之间为待复制正文；实例化时删除两行哨兵、只保留其间内容（房规见 [index.md · 模板边界（哨兵）](index.md#模板边界哨兵)）。

---

<!-- TEMPLATE:BEGIN -->
# L6-{编号}-{文档名称}

| 属性       | 值                             |
| ---------- | ------------------------------ |
| 文档编码   | L6-{编号}                      |
| 层级       | L6 - 验证与确认                |
| 版本       | v1.0                           |
| 状态       | 初稿        |
| 作者       | {当前用户.作者}                         |
| 创建日期   | YYYY-MM-DD                     |
| 最后更新   | YYYY-MM-DD                     |
| 关联文档   | L2-{编号}、L5-{编号} |

> **编码形式**：默认使用简洁编码（`{类型码}-{三位序号}`，与正文占位符一致）。
> **项目前缀**：仅在当前作用域 `README.md` 已声明 `project_code` 且请求发起方明确确认启用时，在各类编码前加 `{项目编码}-` 前缀（细则见 `references/coding-system.md`）。

## 1. 测试策略概述

描述整体测试方针和目标。

<a id="rul-{编号}"></a>
### RUL-{编号}：验证总策略

- **细项状态**：初稿
- **修订版本号**：1
- **最后修订日期**：YYYY-MM-DD
- **策略目标**：{验证目标}
- **覆盖范围**：{覆盖边界}
- **准入条件**：{进入验证的前提}
- **退出条件**：{验证完成判定}

### 1.1 测试范围

- **功能范围**：{功能测试范围}
- **非功能范围**：{非功能测试范围}
- **测试环境**：{环境要求}

### 1.2 测试原则

- **测试优先级**：P0 > P1 > P2
- **通过标准**：{通过条件}
- **失败处理**：{失败处理流程}

## 2. 功能测试

<a id="tc-{编号}"></a>
### TC-{编号}：{测试用例名称}

- **细项状态**：初稿
- **修订版本号**：1
- **最后修订日期**：YYYY-MM-DD
- **测试类型**：功能/集成/系统
- **前置条件**：{前置条件}
- **测试步骤**：
  1. {步骤一}
  2. {步骤二}
  3. {步骤三}
- **预期结果**：{预期结果}
- **验收标准**：[AC-{编号}（{验收标准名称}）]({L2 文档相对路径}#ac-{编号})
- **来源**：[FR-{编号}（{需求标题}）]({L2 文档相对路径}#fr-{编号})

> **逐项重复本块**：每个 `TC` 独立成一个完整定义块，MUST NOT 用连续列表项罗列多个编码。

## 3. 非功能测试

### 3.1 性能测试

- **来源**：[NFR-{编号}（{非功能需求标题}）]({L2 文档相对路径}#nfr-{编号})
- **测试目标**：{性能基线}
- **测试方法**：{方法论}

### 3.2 安全测试

- **来源**：[NFR-{编号}（{非功能需求标题}）]({L2 文档相对路径}#nfr-{编号})
- **测试目标**：{安全基线}
- **测试方法**：{方法论}

## 4. 测试环境

### 4.1 环境配置

| 环境类型 | 用途 | 配置说明 |
| -------- | ---- | -------- |
| 开发环境 | 单元测试 | {配置} |
| 测试环境 | 集成测试 | {配置} |
| 预生产环境 | 系统测试 | {配置} |

### 4.2 测试数据

- **数据来源**：{数据来源}
- **数据准备**：{准备方法}
- **数据清理**：{清理策略}

## 5. 需求追溯矩阵

> **定位**：L6 是**设计文档**层级，本矩阵只记录需求与验证要素、设计要素之间的**追溯覆盖关系**，**不记录测试执行结果**（通过 / 失败 / 缺陷数属测试管理系统职责）。
> **追溯状态**：记录型字段，不参与冻结、不解冻不变号；取值 `未追溯 / 已追溯 / 不适用`。

| 需求编码     | 需求标题   | 验收标准 / 测试场景   | 设计要素   | 追溯状态   | 备注 |
| ------------ | ---------- | --------------------- | ---------- | ---------- | ---- |
| FR-{编号}    | {标题}     | AC-{编号} / TC-{编号}  | L4-{编号}  | 未追溯     |      |
| NFR-{编号}   | {标题}     | AC-{编号} / TC-{编号}  | L5-{编号}  | 未追溯     |      |

## 6. 缺陷管理策略

<a id="rul-{编号}"></a>
### RUL-{编号}：{规则名称}

- **细项状态**：初稿
- **修订版本号**：1
- **最后修订日期**：YYYY-MM-DD
- **规则内容**：{缺陷分级规则 / 响应时效规则 / 回归测试规则}
- **适用范围**：{生效对象与不适用范围}
- **触发条件**：{何时生效}
- **判定口径**：{可检查的阈值、时限或状态集合}

> **逐项重复本块**：缺陷分级、响应时效、回归测试三类规则各占一个 `RUL` 编码，每个独立成一个完整定义块，MUST NOT 用连续列表项罗列多个编码。

---

## 本文档细项编码清单（仅列出本文档定义/包含）

> **清单范围**：仅收录**本文档**定义或包含的细项，**不是** `ued/` 下的全局完整清单。
> **分配流程**：分配编码前必须先检索当前作用域 `README.md` 的全局编码索引，确认无冲突后再分配；分配后同步更新该索引。
> **状态列**：`细项状态` 为必备列，取值 `初稿/正式/草案/废弃`，新建默认 `初稿`（缺字段判定同上）；锁定与解冻规则见 `references/status-definitions.md`。

| 编码       | 类型     | 标题     | 细项状态 |
| ---------- | -------- | -------- | -------- |
| RUL-{编号}  | 规则 | {标题}   | 初稿    |
| TC-{编号}   | 测试场景 | {标题}   | 初稿    |

## 变更记录

| 版本 | 日期       | 作者   | 变更说明 |
| ---- | ---------- | ------ | -------- |
| v1.0 | YYYY-MM-DD | {当前用户.作者} | 初始版本 |
<!-- TEMPLATE:END -->
