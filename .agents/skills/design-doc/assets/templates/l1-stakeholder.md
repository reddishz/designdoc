> **模板：L1 - 利益相关者需求**
>
> **使用说明**
>
> 1. 替换 `{编号}`、`{文档名称}` 等占位符
> 2. 状态值从 `初稿/正式/草案/废弃` 中选择；**新建默认 `初稿`**（缺字段时按 `变更记录` 判定：有定稿条目 → `草案`，从未定稿 → `初稿`，无法判定 → `草案`）
> 3. **编码形式与项目前缀**：见文首说明
> 4. **本文档细项编码清单**：仅本文档范围；分配前检索当前作用域 `README.md` 全局索引，分配后同步更新
> 5. **细项定义块形态（MANDATORY）**：每个编码的定义位 MUST 写成「`<a id="{编码全小写}"></a>` 锚点行 + `### {编码}：{标题}` 标题行（全角冒号）+ `- **{要素名}**：{值}` 属性行」，MUST NOT 写成 `- **{编码}**：{描述}`；同类多项逐项重复完整块（房规见 [coding-system.md · 细项定义块形态](../../references/coding-system.md#细项定义块形态mandatory)）
> 6. **一个编码只有一个定义位**：《利益相关者识别》是**登记视图**（表格），`STK` 的定义块统一放《用户画像》，MUST NOT 两处重复定义同一编码
> 7. 关联文档引用对应的 L0 文档编码
> 8. 维护细项编码清单和变更记录
>
> **正文边界**：`TEMPLATE:BEGIN` / `TEMPLATE:END` 之间为待复制正文；实例化时删除两行哨兵、只保留其间内容（房规见 [index.md · 模板边界（哨兵）](index.md#模板边界哨兵)）。

---

<!-- TEMPLATE:BEGIN -->
# L1-{编号}-{文档名称}

| 属性       | 值                             |
| ---------- | ------------------------------ |
| 文档编码   | L1-{编号}                      |
| 层级       | L1 - 利益相关者需求            |
| 版本       | v1.0                           |
| 状态       | 初稿        |
| 作者       | {当前用户.作者}                         |
| 创建日期   | YYYY-MM-DD                     |
| 最后更新   | YYYY-MM-DD                     |
| 关联文档   | L0-{编号}                      |

> **编码形式**：默认使用简洁编码（`{类型码}-{三位序号}`，与正文占位符一致）。
> **项目前缀**：仅在当前作用域 `README.md` 已声明 `project_code` 且请求发起方明确确认启用时，在各类编码前加 `{项目编码}-` 前缀（细则见 `references/coding-system.md`）。

## 1. 术语与概念（可选）

若本稿无新增易混名词，可写「无」或删除本节。已有**项目词汇表**时，术语的完整定义由项目词汇表承载，本节只列本稿特有、尚未收入项目词汇表的用语。

| 术语 | 定义 | 备注 |
|------|------|------|
| {术语} | {含义} | 可选：缩写、与项目词汇表对齐 |

## 2. 利益相关者识别

> **登记视图**：本节只登记识别到的编码与概要；`STK` 的**定义块**统一在《用户画像》一节，MUST NOT 在两处重复定义同一编码。

| 编码 | 角色名称 | 代表人/群体 | 关注点 | 影响力 |
|------|----------|-------------|--------|--------|
| STK-{编号} | {角色名称} | {代表} | {核心关注点} | 高/中/低 |

## 3. 用户画像

<a id="stk-{编号}"></a>
### STK-{编号}：{角色名称}

- **细项状态**：初稿
- **修订版本号**：1
- **最后修订日期**：YYYY-MM-DD
- **背景**：{用户背景描述}
- **目标**：[GOL-{编号}（{L0 目标标题}）]({L0 文档相对路径}#gol-{编号})
- **痛点**：{当前痛点}
- **期望**：{对系统的期望}

> **逐项重复本块**：每个 `STK` 独立成一个完整定义块（锚点行 + 标题行 + 具名属性行），MUST NOT 用连续列表项罗列多个编码。

## 4. 核心业务场景

<a id="scn-{编号}"></a>
### SCN-{编号}：{场景名称}

- **细项状态**：初稿
- **修订版本号**：1
- **最后修订日期**：YYYY-MM-DD
- **参与者**：[STK-{编号}（{角色名称}）](#stk-{编号})
- **触发条件**：{何时触发}
- **业务流程**：
  1. {步骤一}
  2. {步骤二}
  3. {步骤三}
- **预期结果**：{业务层面的预期结果}
- **约束条件**：{业务规则和约束}

> **逐项重复本块**：每个 `SCN` 独立成一个完整定义块，MUST NOT 用连续列表项罗列多个编码。

## 5. 高阶约束

<a id="con-{编号}"></a>
### CON-{编号}：{约束名称}

- **细项状态**：初稿
- **修订版本号**：1
- **最后修订日期**：YYYY-MM-DD
- **约束内容**：{约束描述}
- **影响范围**：{受本约束限制的需求与设计对象}
- **出处**：{外部给定方，如法律法规 / 运营环境 / 预算与资源}

> **逐项重复本块**：每个 `CON` 独立成一个完整定义块，MUST NOT 用连续列表项罗列多个编码。高阶约束通常覆盖三类：法律法规合规要求、运营环境约束、预算与资源约束。

## 6. 优先级排序

| 需求项       | 优先级   | 理由 |
| ------------ | -------- | ---- |
| SCN-{编号}   | P0/P1/P2 | {理由} |

---

## 本文档细项编码清单（仅列出本文档定义/包含）

> **清单范围**：仅收录**本文档**定义或包含的细项，**不是** `ued/` 下的全局完整清单。
> **分配流程**：分配编码前必须先检索当前作用域 `README.md` 的全局编码索引，确认无冲突后再分配；分配后同步更新该索引。
> **状态列**：`细项状态` 为必备列，取值 `初稿/正式/草案/废弃`，新建默认 `初稿`（缺字段判定同上）；锁定与解冻规则见 `references/status-definitions.md`。

| 编码       | 类型       | 标题     | 细项状态 |
| ---------- | ---------- | -------- | -------- |
| STK-{编号} | 利益相关者 | {标题}   | 初稿    |
| SCN-{编号} | 场景       | {标题}   | 初稿    |
| CON-{编号} | 约束       | {标题}   | 初稿    |

## 变更记录

| 版本 | 日期       | 作者   | 变更说明 |
| ---- | ---------- | ------ | -------- |
| v1.0 | YYYY-MM-DD | {当前用户.作者} | 初始版本 |
<!-- TEMPLATE:END -->
