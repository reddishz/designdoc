# L1 - 利益相关者需求模板

```markdown
# L1-{编号}-{文档名称}

| 属性       | 值                             |
| ---------- | ------------------------------ |
| 文档编号   | L1-{编号}                      |
| 层级       | L1 - 利益相关者需求            |
| 版本       | v1.0                           |
| 状态       | 草稿/正式/废弃      |
| 作者       | {当前用户.作者}                         |
| 创建日期   | YYYY-MM-DD                     |
| 最后更新   | YYYY-MM-DD                     |
| 关联文档   | L0-{编号}                      |

> **编码形式**：默认使用简洁编码（`{类型码}-{三位序号}`，与正文占位符一致）。
> **项目前缀**：仅在 `ued/.doc-config.json` 已配置 `project_code` 且用户明确确认启用时，在各类编码前加 `{项目编码}-` 前缀（细则见 `references/coding-system.md`）。

## 1. 术语与概念（可选）

若本稿无新增易混名词，可写「无」或删除本节。已有**项目词汇表**时，以项目级文件为准，此处仅列本稿特有用语。

| 术语 | 定义 | 备注 |
|------|------|------|
| {术语} | {含义} | 可选：缩写、与项目词汇表对齐 |

## 2. 利益相关者识别

- **STK-{编号}**：{角色名称}
  - 代表人/群体：{代表}
  - 关注点：{核心关注点}
  - 影响力：高/中/低

## 2. 用户画像

### STK-{编号}：{角色名称}

- **背景**：{用户背景描述}
- **目标**：GOL-{编号}（引用 L0 目标）
- **痛点**：{当前痛点}
- **期望**：{对系统的期望}

## 4. 核心业务场景

### SCN-{编号}：{场景名称}

- **参与者**：STK-{编号}
- **触发条件**：{何时触发}
- **业务流程**：
  1. {步骤一}
  2. {步骤二}
  3. {步骤三}
- **预期结果**：{业务层面的预期结果}
- **约束条件**：{业务规则和约束}

## 5. 高阶约束

- **CON-{编号}**：{法律法规合规要求}
- **CON-{编号}**：{运营环境约束}
- **CON-{编号}**：{预算与资源约束}

## 6. 优先级排序

| 需求项       | 优先级   | 理由 |
| ------------ | -------- | ---- |
| SCN-{编号}   | P0/P1/P2 | {理由} |

---

## 本文档细项编码清单（仅列出本文档定义/包含）

> **清单范围**：仅收录**本文档**定义或包含的细项，**不是** `ued/` 下的全局完整清单。
> **分配流程**：分配编码前必须先检索 `ued/README.md` 中的全局编码索引，确认无冲突后再分配；分配后同步更新该索引。

| 编码       | 类型       | 标题     | 来源章节 |
| ---------- | ---------- | -------- | -------- |
| STK-{编号} | 利益相关者 | {标题}   | 2. 利益相关者识别 / 3. 用户画像 |
| SCN-{编号} | 场景       | {标题}   | 4. 核心业务场景 |
| CON-{编号} | 约束       | {标题}   | 5. 高阶约束 |

## 变更记录

| 版本 | 日期       | 作者   | 变更说明 |
| ---- | ---------- | ------ | -------- |
| v1.0 | YYYY-MM-DD | {当前用户.作者} | 初始版本 |
```

## 使用说明

1. 替换 `{编号}`、`{文档名称}` 等占位符
2. 状态值从 `草稿/正式/废弃` 中选择
3. **编码形式与项目前缀**：见文首说明
4. **本文档细项编码清单**：仅本文档范围；分配前检索 `ued/README.md` 全局索引，分配后同步更新
5. 关联文档引用对应的 L0 文档编号
6. 维护细项编码清单和变更记录
