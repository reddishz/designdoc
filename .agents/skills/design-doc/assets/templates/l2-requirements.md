> **模板：L2 - 系统/产品需求**
>
> **通用规则**：见 [index.md · 整篇型模板共用](index.md#整篇型模板共用)。
>
> **本模板**
>
> 1. 无「本轮不做」的想法时删除《规划项》及清单中的 PLN 行；新建 PLN 的 `落实记录` 写「无」
> 2. `STK` 只在 L1 定义；《用户特征》只摘要并链接
> 3. 无独立业务规则时同时删除：FR 的 `处理规则` 行、《业务规则》定义块、清单中的 RUL 行
> 4. 一条 AC 只服务一类需求：FR 与 NFR 各自建 AC，实例化时分号；NFR 的 AC 其 `来源` 指向该 NFR
> 5. 《对外接口一览》只识别产品通道；契约细节由 L4 IF 的 `来源` 回指 FR，本节不要链到 IF
> 6. 无界面交互时删除《界面需求》
>
> **正文边界**：见 [index.md · 模板边界（哨兵）](index.md#模板边界哨兵)。

---

<!-- TEMPLATE:BEGIN -->
# L2-{编号}-{文档名称}

| 属性       | 值                             |
| ---------- | ------------------------------ |
| 文档编码   | L2-{编号}                      |
| 层级       | L2 - 系统/产品需求             |
| 版本       | v1.0                           |
| 状态       | 初稿        |
| 作者       | {当前用户.作者}                         |
| 创建日期   | YYYY-MM-DD                     |
| 最后更新   | YYYY-MM-DD                     |
| 关联文档   | L1-{编号}                      |

## 1. 概述

简要描述本需求集合的背景和范围，对齐的上层场景：[SCN-{编号}（{场景标题}）]({L1 文档相对路径}#scn-{编号})。

### 1.1 系统上下文（可选）

产品视角下本软件在更大系统中的位置，以及与外部角色、系统的边界。

### 1.2 用户特征（可选）

本需求所面向的角色：

- [STK-{编号}（{角色名称}）]({L1 文档相对路径}#stk-{编号})：{与本稿相关的特征或能力假设}

### 1.3 对外接口一览（可选）

| 名称 | 对象 | 说明 |
| ---- | ---- | -------- |
| {接口或通道名} | {外部系统 / 用户} | {通道用途} |

### 1.4 术语与概念（可选）

需求域核心对象、与外部系统/业务线约定名称、易混缩写；已收入项目词汇表的不列。

| 术语 | 定义 | 备注 |
|------|------|------|
| {术语} | {含义} | {与项目词汇表的关系} |

## 2. 功能需求

<a id="fr-{编号}"></a>
### FR-{编号}：{功能名称}

- **细项状态**：初稿
- **修订版本号**：1
- **最后修订日期**：YYYY-MM-DD
- **优先级**：P0/P1/P2
- **处理规则**：[RUL-{编号}（{规则标题}）](#rul-{编号})
- **验证方式**：[AC-{编号}（{验收标准标题}）](#ac-{编号})
- **来源**：[SCN-{编号}（{场景标题}）]({L1 文档相对路径}#scn-{编号})

当 {触发条件} 时，系统应当 {动作}{对象}，并满足 {可观察约束}。

- 触发与输入：{何时触发、需要哪些输入信息}
- 输出与响应：{预期输出或系统响应}
- 不适用范围：{至少写明一条非目标}

## 3. 非功能需求

<a id="nfr-{编号}"></a>
### NFR-{编号}：{需求名称}

- **细项状态**：初稿
- **修订版本号**：1
- **最后修订日期**：YYYY-MM-DD
- **类别**：性能/安全/可用性/兼容性/可靠性/可维护性/可移植性/易用性
- **优先级**：P0/P1/P2
- **度量标准**：{数值 + 单位 + 比较符，如 P99 ≤ 200ms}
- **验证方式**：[AC-{编号}（{验收标准标题}）](#ac-{编号})
- **来源**：[CON-{编号}（{约束标题}）]({L1 文档相对路径}#con-{编号})

在 {适用边界} 内，系统应当 {质量属性} 达到 {度量标准}。

- 适用范围：{生效的系统边界}
- 判定口径：{在什么条件下、用什么方法度量}
- 不适用范围：{至少写明一条非目标}

## 4. 用例描述

<a id="uc-{编号}"></a>
### UC-{编号}：{用例名称}

- **细项状态**：初稿
- **修订版本号**：1
- **最后修订日期**：YYYY-MM-DD
- **参与者**：[STK-{编号}（{角色名称}）]({L1 文档相对路径}#stk-{编号})
- **来源**：[SCN-{编号}（{场景标题}）]({L1 文档相对路径}#scn-{编号})

{本用例完成什么：一句话说明用户目标。}

主流程：

1. {步骤}
2. {步骤}

| 环节 | 内容 |
|------|------|
| 前置条件 | {进入本用例前必须成立的状态} |
| 备选流程 | {备选路径} |
| 后置条件 | {用例成功结束后的系统状态} |
| 异常流程 | {异常处理与回退路径} |

## 5. 业务规则

<a id="rul-{编号}"></a>
### RUL-{编号}：{规则名称}

- **细项状态**：初稿
- **修订版本号**：1
- **最后修订日期**：YYYY-MM-DD
- **验证方式**：[AC-{编号}（{验收标准标题}）](#ac-{编号})
- **来源**：[FR-{编号}（{需求标题}）](#fr-{编号})

{可执行的业务规则：一句话写明本规则判定什么。}

- 触发条件：{何时生效}
- 判定口径：{可检查的阈值、时限或状态集合}
- 生效对象：{适用范围}
- 不适用范围：{至少写明一条非目标}

## 6. 数据需求

描述涉及的数据实体及其关系（概念层面，非物理模型）。

| 实体     | 关键属性   | 说明 |
| -------- | ---------- | ---- |
| {实体}   | {属性列表} | {说明} |

## 7. 界面需求

描述界面逻辑和交互约束，引用原型链接。

## 8. 验收标准

<a id="ac-{编号}"></a>
### AC-{编号}：{验收标准名称}

- **细项状态**：初稿
- **修订版本号**：1
- **最后修订日期**：YYYY-MM-DD
- **验证方式**：[TC-{编号}（{测试场景标题}）]({L6 文档相对路径}#tc-{编号})
- **来源**：[FR-{编号}（{需求标题}）](#fr-{编号})

{可判定的验收条件：一句话写明什么情况下算验收通过。}

- 阈值与时限：{可检查的数值、时限或状态集合}
- 判定方式：{如何观察与判定}

## 9. 约束与假设

<a id="con-{编号}"></a>
### CON-{编号}：{约束名称}

- **细项状态**：初稿
- **修订版本号**：1
- **最后修订日期**：YYYY-MM-DD
- **取值范围**：{区间或集合表达式，如 [0.1, 10]}
- **出处**：{外部给定方，如法规 / 平台 / 既有系统 / 合同}

{本约束限定什么：一句话写明约束内容。}

- 影响范围：{受本约束限制的需求与设计对象}
- 不适用范围：{至少写明一条非目标}

<a id="asm-{编号}"></a>
### ASM-{编号}：{假定名称}

- **细项状态**：初稿
- **修订版本号**：1
- **最后修订日期**：YYYY-MM-DD
- **出处**：{假定依据的现实出处，如用户访谈 / 市场调研 / 既有系统行为}

{设计所依赖的前提假设：一句话写明假定什么成立。}

- 适用范围：{依赖本假定的需求与设计对象}
- 失效影响：{假定不成立时的影响与应对}

## 10. 规划项

<a id="pln-{编号}"></a>
### PLN-{编号}：{想法标题}

- **细项状态**：初稿
- **修订版本号**：1
- **最后修订日期**：YYYY-MM-DD
- **优先级**：高 / 中 / 低
- **预期落点**：{将来可能落实为的类型码，如 FR / NFR / PRN}
- **落实情况**：未落实
- **登记日期**：YYYY-MM-DD
- **建议复审日期**：YYYY-MM-DD
- **落实记录**：无
- **出处**：{利益相关者 / 用户反馈 / 评审遗留 等}

{这个能力解决什么问题、为谁带来什么价值。}

- 当前不做的原因：{为什么本轮不实现}
- 重新考虑的条件：{出现什么信号时值得再次评估}

---

## 本文档细项编码清单（仅列出本文档定义/包含）

| 编码       | 类型     | 标题     | 细项状态 |
| ---------- | -------- | -------- | -------- |
| FR-{编号}  | 功能需求 | {标题}   | 初稿    |
| NFR-{编号} | 非功能需求 | {标题} | 初稿    |
| UC-{编号}  | 用例     | {标题}   | 初稿    |
| RUL-{编号} | 规则     | {标题}   | 初稿    |
| AC-{编号}  | 验收标准 | {标题}   | 初稿    |
| CON-{编号} | 约束     | {标题}   | 初稿    |
| ASM-{编号} | 假定     | {标题}   | 初稿    |
| PLN-{编号} | 规划项   | {标题}   | 初稿    |

## 变更记录

| 版本 | 日期       | 作者   | 变更说明 |
| ---- | ---------- | ------ | -------- |
| v1.0 | YYYY-MM-DD | {当前用户.作者} | 初始版本 |
<!-- TEMPLATE:END -->
