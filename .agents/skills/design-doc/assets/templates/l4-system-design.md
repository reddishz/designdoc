# L4 - 逻辑/系统设计模板

```markdown
# L4-{编号}-{文档名称}

| 属性       | 值                             |
| ---------- | ------------------------------ |
| 文档编号   | L4-{编号}                      |
| 层级       | L4 - 逻辑/系统设计             |
| 版本       | v1.0                           |
| 状态       | 草稿/正式/废弃      |
| 作者       | {当前用户.作者}                         |
| 创建日期   | YYYY-MM-DD                     |
| 最后更新   | YYYY-MM-DD                     |
| 关联文档   | L3-{编号}                      |

> **编码形式**：默认使用简洁编码（`{类型码}-{三位序号}`，与正文占位符一致）。
> **项目前缀**：仅在 `ued/.doc-config.json` 已配置 `project_code` 且用户明确确认启用时，在各类编码前加 `{项目编码}-` 前缀（细则见 `references/coding-system.md`）。

## 1. 设计概述

描述本模块/服务在系统中的定位和设计目标。
所属组件：CMP-{编号}（引用 L3 组件）
遵循原则：PRN-{编号}（引用 L3 原则）

### 1.1 术语与概念（可选）

本设计范围内的模块/接口简称、与领域模型用语对齐；若与上层文档一致，可写「同 `L3-{编号}`」或「见项目词汇表」。

| 术语 | 定义 | 备注 |
|------|------|------|
| {术语} | {含义} | 可选 |

## 2. 模块设计

### 2.1 职责划分

- **核心职责**：{职责描述}
- **边界定义**：{边界说明}
- **依赖关系**：{依赖的其他模块}

### 2.2 接口设计

#### IF-{编号}：{接口名称}

- **类型**：REST API / 消息队列 / 内部接口
- **描述**：{接口功能描述}
- **输入**：{输入参数}
- **输出**：{输出格式}
- **异常处理**：{异常情况}

## 3. 数据设计

### 3.1 领域模型

```mermaid
classDiagram
    class 订单 {
        订单号
        状态
        创建时间
    }
    class 订单项 {
        商品ID
        数量
        单价
    }
    订单 "1" -- "*" 订单项 : 包含
```

### 3.2 数据流转

描述关键数据在模块内的流转过程。

## 4. 异常处理与容错

| 场景     | 影响分析   | 处理策略     | 关联风险 |
| -------- | ---------- | ------------ | -------- |
| {场景}   | {影响}     | {策略}       | RSK-{编号} |

## 5. 性能与扩展性

- **性能目标**：{性能指标}
- **扩展策略**：{扩展方案}
- **容量规划**：{容量考虑}

---

## 本文档细项编码清单（仅列出本文档定义/包含）

> **清单范围**：仅收录**本文档**定义或包含的细项，**不是** `ued/` 下的全局完整清单。
> **分配流程**：分配编码前必须先检索 `ued/README.md` 中的全局编码索引，确认无冲突后再分配；分配后同步更新该索引。

| 编码       | 类型     | 标题     | 来源章节 |
| ---------- | -------- | -------- | -------- |
| IF-{编号}  | 接口     | {标题}   | 2.2 接口设计 |
| DOM-{编号} | 领域对象 | {标题}   | 3. 数据设计 |

## 变更记录

| 版本 | 日期       | 作者   | 变更说明 |
| ---- | ---------- | ------ | -------- |
| v1.0 | YYYY-MM-DD | {当前用户.作者} | 初始版本 |
```

## 使用说明

1. 替换 `{编号}`、`{文档名称}` 等占位符
2. 状态值从 `草稿/正式/废弃` 中选择
3. **编码形式与项目前缀**：见文首说明
4. **本文档细项编码清单**：仅本文档范围；分配前检索 `ued/README.md` 全局索引，分配后同步更新
5. 关联文档引用对应的 L3 文档编号
6. Mermaid 图表可根据实际设计调整
