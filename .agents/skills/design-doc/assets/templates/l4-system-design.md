> **模板：L4 - 逻辑/系统设计**
>
> **使用说明**
>
> 1. 替换 `{编号}`、`{文档名称}` 等占位符
> 2. 状态值从 `初稿/正式/草案/废弃` 中选择；**新建默认 `初稿`**（缺字段时按 `变更记录` 判定：有定稿条目 → `草案`，从未定稿 → `初稿`，无法判定 → `草案`）
> 3. **编码形式与项目前缀**：见文首说明
> 4. **本文档细项编码清单**：仅本文档范围；分配前检索当前作用域 `README.md` 全局索引，分配后同步更新
> 5. **细项定义块形态（MANDATORY）**：每个编码的定义位 MUST 写成「`<a id="{编码全小写}"></a>` 锚点行 + `#### {编码}：{标题}` 标题行（全角冒号，层级比父小节深一级）+ `- **{要素名}**：{值}` 属性行」，MUST NOT 写成 `- **{编码}**：{描述}`，也 MUST NOT 写成裸段落 `所属组件：…`（房规见 [coding-system.md · 细项定义块形态](../../references/coding-system.md#细项定义块形态mandatory)）
> 6. 关联文档引用对应的 L3 文档编码
> 7. Mermaid 图表可根据实际设计调整
>
> **正文边界**：`TEMPLATE:BEGIN` / `TEMPLATE:END` 之间为待复制正文；实例化时删除两行哨兵、只保留其间内容（房规见 [index.md · 模板边界（哨兵）](index.md#模板边界哨兵)）。

---

<!-- TEMPLATE:BEGIN -->
# L4-{编号}-{文档名称}

| 属性       | 值                             |
| ---------- | ------------------------------ |
| 文档编码   | L4-{编号}                      |
| 层级       | L4 - 逻辑/系统设计             |
| 版本       | v1.0                           |
| 状态       | 初稿        |
| 作者       | {当前用户.作者}                         |
| 创建日期   | YYYY-MM-DD                     |
| 最后更新   | YYYY-MM-DD                     |
| 关联文档   | L3-{编号}                      |

> **编码形式**：默认使用简洁编码（`{类型码}-{三位序号}`，与正文占位符一致）。
> **项目前缀**：仅在当前作用域 `README.md` 已声明 `project_code` 且请求发起方明确确认启用时，在各类编码前加 `{项目编码}-` 前缀（细则见 `references/coding-system.md`）。
> **表达强制约束**：**彻底禁止**使用章节编号（如 `§1.4.3`）进行任何语义引用（包括规则、流程、算法、背景、说明、结论）；所有引用一律使用细项编码或文档编码。

## 1. 设计概述

描述本模块/服务在系统中的定位和设计目标。

- **所属组件**：[CMP-{编号}（{组件名称}）]({L3 文档相对路径}#cmp-{编号})
- **遵循原则**：[PRN-{编号}（{原则标题}）]({L3 文档相对路径}#prn-{编号})

### 1.1 术语与概念（可选）

本设计范围内的模块/接口简称、与领域模型用语对齐；若与上层文档一致，可写「同 `L3-{编号}`」或「见项目词汇表」。

| 术语 | 定义 | 备注 |
|------|------|------|
| {术语} | {含义} | 可选 |

## 2. 模块设计

### 2.1 职责划分

- **核心职责**：{职责描述}
- **边界定义**：{边界说明}
- **依赖关系**：{依赖的其他模块}

### 2.2 接口设计

<a id="if-{编号}"></a>
#### IF-{编号}：{接口名称}

- **细项状态**：初稿
- **修订版本号**：1
- **最后修订日期**：YYYY-MM-DD
- **类型**：REST API / 消息队列 / 内部接口
- **描述**：{接口功能描述}
- **输入**：{输入参数}
- **输出**：{输出格式}
- **异常处理**：{异常情况}
- **来源**：[FR-{编号}（{需求标题}）]({L2 文档相对路径}#fr-{编号})

> **逐项重复本块**：每个 `IF` 独立成一个完整定义块，MUST NOT 用连续列表项罗列多个编码。

## 3. 数据设计

### 3.1 领域模型

```mermaid
classDiagram
    class 订单 {
        订单号
        状态
        创建时间
    }
    class 订单项 {
        商品ID
        数量
        单价
    }
    订单 "1" -- "*" 订单项 : 包含
```

> **图不代替定义块**：`classDiagram` 只表达实体间关系（登记视图）；每个入库实体仍 MUST 在下方各占一个完整 `DOM` 定义块。

<a id="dom-{编号}"></a>
#### DOM-{编号}：{领域对象名称}

- **细项状态**：初稿
- **修订版本号**：1
- **最后修订日期**：YYYY-MM-DD
- **定义**：{该实体的业务含义}
- **关键属性**：{属性及其业务约束}
- **边界定义**：{适用范围}；不包括 {非目标}
- **所属组件**：[CMP-{编号}（{组件名称}）]({L3 文档相对路径}#cmp-{编号})
- **来源**：[FR-{编号}（{需求标题}）]({L2 文档相对路径}#fr-{编号})

> **逐项重复本块**：每个 `DOM` 独立成一个完整定义块，MUST NOT 用连续列表项罗列多个编码。

### 3.2 数据流转

描述关键数据在模块内的流转过程。

## 4. 异常处理与容错

| 场景     | 影响分析   | 处理策略     | 关联风险 |
| -------- | ---------- | ------------ | -------- |
| {场景}   | {影响}     | {策略}       | RSK-{编号} |

## 5. 性能与扩展性

- **性能目标**：{性能指标}
- **扩展策略**：{扩展方案}
- **容量规划**：{容量考虑}

---

## 本文档细项编码清单（仅列出本文档定义/包含）

> **清单范围**：仅收录**本文档**定义或包含的细项，**不是** `ued/` 下的全局完整清单。
> **分配流程**：分配编码前必须先检索当前作用域 `README.md` 的全局编码索引，确认无冲突后再分配；分配后同步更新该索引。
> **状态列**：`细项状态` 为必备列，取值 `初稿/正式/草案/废弃`，新建默认 `初稿`（缺字段判定同上）；锁定与解冻规则见 `references/status-definitions.md`。

| 编码       | 类型     | 标题     | 细项状态 |
| ---------- | -------- | -------- | -------- |
| IF-{编号}  | 接口     | {标题}   | 初稿    |
| DOM-{编号} | 领域对象 | {标题}   | 初稿    |

## 变更记录

| 版本 | 日期       | 作者   | 变更说明 |
| ---- | ---------- | ------ | -------- |
| v1.0 | YYYY-MM-DD | {当前用户.作者} | 初始版本 |
<!-- TEMPLATE:END -->
