> **模板：L4 - 逻辑/系统设计**
>
> **通用规则**：见 [index.md · 整篇型模板共用](index.md#整篇型模板共用)。
>
> **本模板**
>
> 1. `classDiagram` 只画关系；每个入库实体仍须各占一个 `DOM` 定义块
> 2. 无独立业务动作时删除 ACT 定义块、清单行，以及 IF 正文的「触发动作」行；有 ACT 时其 `依赖` 指向对应 IF
> 3. 模块运行前提写在 L3 对应 CMP 的 `依赖` 行，不在本稿无编码复述
> 4. 本稿把 L3 的组织落成契约与结构；算法、配置与模块内流程写 L5。MUST NOT 在本稿定义 FR / NFR / AC / UC
>
> **正文边界**：见 [index.md · 模板边界（哨兵）](index.md#模板边界哨兵)。

---

<!-- TEMPLATE:BEGIN -->
# L4-{编号}-{文档名称}

| 属性       | 值                             |
| ---------- | ------------------------------ |
| 文档编码   | L4-{编号}                      |
| 层级       | L4 - 逻辑/系统设计             |
| 版本       | v1.0                           |
| 状态       | 初稿        |
| 作者       | {当前用户.作者}                         |
| 创建日期   | YYYY-MM-DD                     |
| 最后更新   | YYYY-MM-DD                     |
| 关联文档   | L3-{编号}                      |

## 1. 设计概述

描述本模块/服务在系统中的定位和设计目标。本模块归属 [CMP-{编号}（{组件名称}）]({L3 文档相对路径}#cmp-{编号})，设计上遵循 [PRN-{编号}（{原则标题}）]({L3 文档相对路径}#prn-{编号})。

### 1.1 术语与概念（可选）

本设计范围内的模块/接口简称、与领域模型用语对齐；与上层文档一致时写「同 [L3-{编号}（{架构文档标题}）]({L3 文档相对路径})」。

| 术语 | 定义 | 备注 |
|------|------|------|
| {术语} | {含义} | 可选 |

## 2. 模块设计

### 2.1 职责划分

- 核心职责：{职责描述}
- 边界定义：{边界说明}

### 2.2 接口设计

<a id="if-{编号}"></a>
#### IF-{编号}：{接口名称}

- **细项状态**：初稿
- **修订版本号**：1
- **最后修订日期**：YYYY-MM-DD
- **类型**：REST API / 消息队列 / 内部接口 / 事件
- **所属组件**：[CMP-{编号}（{组件名称}）]({L3 文档相对路径}#cmp-{编号})
- **来源**：[FR-{编号}（{需求标题}）]({L2 文档相对路径}#fr-{编号})

{本接口提供什么：一句话写明接口功能。}

| 环节 | 内容 |
|------|------|
| 输入 | {输入参数} |
| 输出 | {输出格式} |
| 异常处理 | {异常情况与回退策略} |

- 触发动作：[ACT-{编号}（{动作名称}）](#act-{编号})

### 2.3 动作设计

<a id="act-{编号}"></a>
#### ACT-{编号}：{动作名称}

- **细项状态**：初稿
- **修订版本号**：1
- **最后修订日期**：YYYY-MM-DD
- **来源**：[FR-{编号}（{需求标题}）]({L2 文档相对路径}#fr-{编号})
- **依赖**：[IF-{编号}（{接口名称}）](#if-{编号})

{本动作推进什么：一句话写明业务状态变化。}

- 触发条件：{何时执行}
- 前置状态：{执行前必须成立}
- 结果状态：{执行后的系统状态}

## 3. 数据设计

### 3.1 领域模型

```mermaid
classDiagram
    class 订单 {
        订单号
        状态
        创建时间
    }
    class 订单项 {
        商品ID
        数量
        单价
    }
    订单 "1" -- "*" 订单项 : 包含
```

<a id="dom-{编号}"></a>
#### DOM-{编号}：{领域对象名称}

- **细项状态**：初稿
- **修订版本号**：1
- **最后修订日期**：YYYY-MM-DD
- **所属组件**：[CMP-{编号}（{组件名称}）]({L3 文档相对路径}#cmp-{编号})
- **来源**：[FR-{编号}（{需求标题}）]({L2 文档相对路径}#fr-{编号})

{该实体的业务含义：一句话写明本领域对象代表什么。}

| 关键属性 | 业务约束 |
|----------|----------|
| {属性} | {约束} |

- 适用范围：{边界说明}
- 不适用范围：{至少写明一条非目标}

### 3.2 数据流转

描述关键数据在模块内的流转过程。

## 4. 异常处理与容错

| 场景     | 影响分析   | 处理策略     | 关联风险 |
| -------- | ---------- | ------------ | -------- |
| {场景}   | {影响}     | {策略}       | RSK-{编号} |

## 5. 性能与扩展性

容量与时延目标对齐 [NFR-{编号}（{非功能需求标题}）]({L2 文档相对路径}#nfr-{编号})。

- 扩展策略：{在该 NFR 约束下的扩展方案}
- 容量规划：{容量考虑}

---

## 本文档细项编码清单（仅列出本文档定义/包含）

| 编码       | 类型     | 标题     | 细项状态 |
| ---------- | -------- | -------- | -------- |
| IF-{编号}  | 接口     | {标题}   | 初稿    |
| ACT-{编号} | 动作     | {标题}   | 初稿    |
| DOM-{编号} | 领域对象 | {标题}   | 初稿    |

## 本文引用

| 引用对象 | 类型 | 所在文档 | 钉住代际 | 钉住日期 |
|----------|------|----------|----------|----------|

## 变更记录

| 版本 | 日期       | 作者   | 变更说明 |
| ---- | ---------- | ------ | -------- |
| v1.0 | YYYY-MM-DD | {当前用户.作者} | 初始版本 |
<!-- TEMPLATE:END -->
